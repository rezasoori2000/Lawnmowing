{
  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "DefaultConnection": ""
  },
  "Jwt": {
    "SigningKey": ""
  },
  "Cors": {
    "AllowedOrigins": []
  },
  "_comment": "Production secrets (ConnectionStrings:DefaultConnection, Jwt:SigningKey) must be supplied via environment variables (ConnectionStrings__DefaultConnection, Jwt__SigningKey) or a secrets manager (e.g. Azure Key Vault / AWS Secrets Manager) - never committed here. Cors:AllowedOrigins must list the mobile app's real production origin(s)."
}
